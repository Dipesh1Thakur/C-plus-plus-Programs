#include<stdio.h>
#include<conio.h>
int main()
{
	char status;
	int year of services of , salary;
	printf("Enter the employee status full time 'f' and part time'p'\n ");
	scanf("%d",& employee status );
	printf("Enter year of services from employee");
	scanf("%d",&employee_salary);
	switch(employee_status)
	{
		case'F'=
		 if(year of service>5)
		{
	    new-salary=employee_salary + emppolyee_salary * 0.05;	
	    }                                             
     	printf("%d",new_salary);
        }
        else
        {
        	new_salary= employee_salary+employee_salary * 0.04; 
		}
			break;
			case 'p'= 
			if(  )
		}
	    }
        }
