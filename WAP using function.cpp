#include<stdio.h>
#include<conio.h>
int fun(char A,int x,int b);
int main()
{
	int A,x,b,i,y;
	printf("Enter the character to printedln");
	scanf("%c",&A);
	printf("Enter the number of times that character to be printed on a line ");
	scanf("%d",x);
		printf("Enter the number of lines  to be printe ");
	scanf("%d",b);
	for(i=0;i<y;i++)
	{
		printf("%c",A);
		
	
	printf("\n");
}
fun(A,x,b);
getch();
return(0);
}
